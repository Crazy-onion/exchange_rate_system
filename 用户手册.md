# 汇率波动看板 · 用户手册

> 面向部门同事。打开网址即可使用，无需登录、无需安装。

## 一、网址与访问
- **看板地址**：https://Crazy-onion.github.io/exchange_rate_system/
- 任何电脑、手机浏览器直接打开即可（建议用 Chrome / Edge / Safari）。
- 同事之间可把链接直接转发，无需权限。

## 二、页面由哪几块组成
1. **抬头**：显示「自动更新时间」「手动更新时间」，以及「下载当前月份底稿」按钮。
2. **印尼央行汇率（IDR）**：CNY 人民币中间价、USD 美元中间价（印尼央行发布）。
3. **人民币汇率中间价（SAFE）**：分两个区——
   - 直接标价法（100 外币折合人民币）：美元、欧元、日元、港元、英镑、澳元、新西兰元、新加坡元、瑞士法郎、加元
   - 间接标价法（100 人民币折合外币）：澳门元、林吉特、卢布、兰特、韩元、迪拉姆、里亚尔、福林、兹罗提、丹麦克朗、瑞典克朗、挪威克朗、里拉、比索、泰铢
4. **日期选择**：选定一个「截止日」，查看该日及之前的数据。
5. **汇率趋势图**：选币种 + 时间范围，看走势与统计指标。
6. **历史数据明细**：表格列出每日汇率，可导出 CSV。
7. **手动实时更新按钮**：刷新本页数据（详见第八节）。

## 三、怎么读汇率卡片
- 每张卡片显示：**币种代码 / 名称 / 汇率值 / 报价方式备注 / 数据日期**。
- **报价方式**
  - 直接标价法：100 外币 = 多少人民币（如 100 USD = 678.99 CNY）
  - 间接标价法：100 人民币 = 多少外币（如 100 CNY = 119.00 MOP）
- **数据日期**：每张卡下方 `数据日期: X` 表示这张卡用的**该币种自己真实发布的那一天**的数据，不一定是你看的截止日。
- **滞后标红**：如果某币种当天（截止日）的汇率还没发布，系统会自动取「截止日或之前最近一个已发布工作日」的值，并把卡片标红、显示 `(滞后)`，区块标题也会汇总列出哪些币种滞后。这样你能一眼看出"哪些数字不是当天的"。

## 四、日期选择器怎么用
- 选任意「截止日」，页面所有卡片、图表、明细都按该日（及之前）的数据展示。
- 若该日无数据（如节假日），系统自动取最近一个工作日的数据。
- 可选范围：从最早有数据的那天，到**今天**（不会让你选到未来）。

## 五、汇率趋势图
- 顶部下拉选「币种」（含印尼央行 CNY/USD、25 个人民币中间价）。
- 下方选时间范围：近 30 天 / 90 天 / 180 天 / 1 年 / 全部。
- 统计区显示：最新值、期间变动 %、最高、最低、平均、**最新数据日期**。

## 六、历史数据明细与 CSV 导出
- 表格列：日期 | 币种 | 汇率（币种列含报价方式备注）。
- **导出单一币种 CSV**：只导出当前所选币种的历史。
- **导出全部币种 CSV**：导出所有币种、所有日期的明细。
- CSV 是在你浏览器里用「当前页面已加载的数据」现场生成的，**始终等于页面现在显示的数据**（详见管理员手册「数据新鲜度」）。

## 七、下载 Excel 底稿
- 页面提供「下载当前月份底稿」按钮，下载的是形如 `汇率底稿_202607.xlsx` 的 Excel。
- 系统一共维护**最近 13 个月**的月度底稿（2025-07 ～ 2026-07），在页面「汇率趋势图」下方或下载区可切换月份。
- **Excel 随每天自动更新一起重新生成**（见管理员手册）；你下载到的就是最近一次自动更新算出来的版本。
- ⚠️ 注意：网页上的「手动实时更新」按钮**不会**更新 Excel（它只刷新页面显示）。想要最新 Excel，需等每天自动更新，或请管理员手动触发一次更新（见下一节与管理员手册）。

## 八、手动实时更新按钮
- 点击后，页面会**重新拉取服务器上「已部署的最新数据」并重绘**（显示「更新中…」→ 写入手动更新时间）。
- 它**不重新抓取网站、也不重新生成 Excel**——只是把已经部署好的数据再读一遍。
- 如果数据没有变化（管理员还没触发新的更新），点了也不会出现新数字，这是正常的。
- **真正让 Excel 也刷新**：需管理员到 GitHub Actions 手动触发一次更新（步骤见管理员手册）。按钮下方也有提示文案引导。

## 九、常见问题
**Q：为什么某个币种是红色、写着「滞后」？**
A：表示该币种在你选的截止日还没发布汇率，系统取了更早一个工作日的数值，并已标红提醒，避免你误以为是当天数据。

**Q：为什么日期选择器最多只能选到今天？**
A：因为未来的汇率还没发布。系统总是按「已发布数据」展示，上限设为今天。

**Q：数据到底算哪一天的？**
A：每张卡片都标了「数据日期」，就是该币种真实发布日；若某天某币种缺失，会顺延到最近一个工作日并标红。

**Q：手机能用吗？**
A：能。手机浏览器打开网址即可查看、选日期、导出 CSV、下载 Excel。

**Q：想立刻拿到最新 Excel 怎么办？**
A：联系管理员（维护人）在 GitHub Actions 触发一次更新；或等当天 18:00 自动更新。
