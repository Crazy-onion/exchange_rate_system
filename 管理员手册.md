# 汇率底稿系统 · 管理员手册

> 面向系统维护人（GitHub 仓库 owner）。涵盖架构、更新操作、发布流程，以及**完整数据逻辑**。

## 一、系统架构
- **托管**：GitHub Pages（静态站点，URL 永久稳定）
- **调度**：GitHub Actions 工作流 `.github/workflows/deploy.yml`
  - 定时：每个工作日 **18:00 北京时间**（cron `0 10 * * 1-5` UTC）
  - 推送触发：每次 `git push` 自动重跑
  - 手动触发：`workflow_dispatch`（Run workflow）
- **数据源**（详见第六节）
  1. Ortax 印尼央行：CNY / USD 中间价（IDR）
  2. SAFE 货币折算率：月末各货币对美元折算率
  3. SAFE 人民币中间价：每日 25 币种中间价
- **产出**
  - 13 个月度 Excel 底稿 `汇率底稿_YYYYMM.xlsx`（2025-07 ～ 2026-07）
  - 看板 `index.html` + `data.json`（25 币种明细 + 印尼央行 + 折算率）
- **仓库**：`Crazy-onion/exchange_rate_system`
- **站点**：https://Crazy-onion.github.io/exchange_rate_system/

## 二、仓库结构
```
exchange_rate_system/
├── main.py                  # 主入口：抓取→生成13个月Excel→生成看板
├── scrapers.py              # 三个数据源抓取
├── holidays.py              # 中国公众假期定义
├── excel_generator.py       # 7-Sheet Excel 生成（支持任意月份）
├── dashboard.py             # HTML 看板生成（含数据逻辑/标红）
├── regen_all.py             # 从缓存重新生成（不重新抓取）
├── regen_dashboard.py       # 仅重新生成看板
├── config.json              # update_worker_url（当前为空，未启用云端按钮）
├── requirements.txt         # 依赖
├── template/汇率底稿模版.xlsx  # 云端可用的 Excel 模板
├── .github/workflows/deploy.yml
├── dist/                    # 输出目录（被 .gitignore 忽略，由工作流生成）
├── cache/                   # 数据缓存（被忽略）
├── cloudflare-worker.js     # （可选）云端一键更新 Worker，当前未启用
├── scf_trigger.py           # （可选）腾讯云函数版，当前未启用
├── 用户手册.md
└── README.md
```

## 三、自动更新机制
- 默认分支 `main`。
- 工作流在三种情况下运行：
  1. **定时**：UTC 10:00 周一至周五 = 北京 18:00 工作日
  2. **push**：本地改代码并 Push 后自动重部署
  3. **手动**：仓库 Actions 页点 Run workflow

## 四、手动触发更新（Run workflow）
**电脑浏览器**
1. 打开 github.com → 进 `exchange_rate_system` 仓库 → **Actions**
2. 左侧点 **「汇率底稿自动更新」**
3. 右上角 **Run workflow** → 分支选 `main` → 再点 **Run workflow**
4. 等 build + deploy 变绿勾（约 1–3 分钟），站点即刷新，Excel 同步更新。

**手机浏览器**（github.com 加载不稳时）
1. 手机打开 github.com 并登录
2. 进仓库 → **Actions** → 点工作流 → **Run workflow** → 选 `main` → 运行
3. 若页面挤压点不到按钮：Safari 点地址栏左侧「大小」图标 → **请求桌面网站**；Chrome 点 ⋮ → **桌面版网站**
4. 等待绿勾即可。

> 说明：在 GitHub Actions 触发 = 重新抓取三个网站 + 重算 13 个月 Excel + 重发布。这与每天自动跑的是同一条流水线，Excel 一定是最新的。

## 五、修改代码后发布
1. 本地修改代码（如 `dashboard.py` / `scrapers.py`）
2. 用 GitHub Desktop **Commit**（写清楚改动）
3. 点 **Push origin** → 推送触发工作流自动重部署
4. 约 1–3 分钟后线上生效（`Ctrl+F5` 强刷看板）

## 六、数据逻辑详解（核心）

### 1. 三大数据源及抓取
| 源 | 内容 | 抓取方式 | 频率/时点 |
|----|------|----------|-----------|
| Ortax 印尼央行 | CNY、USD 对 IDR 中间价 | 解析 Next.js 内嵌 JSON（正则提日期+Rp 值），印尼格式 `Rp2.646,14`=2646.14 | 工作日发布 |
| SAFE 货币折算率 | 月末各货币对美元折算率表 | 列表页→详情页→下载 xlsx 解析 | 月末 |
| SAFE 人民币中间价 | 每日 25 币种中间价 | AJAX POST 接口（startDate/endDate/queryYN） | 每日 |

### 2. 日期与节假日规则
- **中国公众假期**（`holidays.py` 定义）：用于判断某日是否为工作日。
- **月初规则**：若所取月初日恰逢节假日/非工作日，取**上月最后一个工作日**的值。
- **月末规则**：若所取月末日恰逢节假日/非工作日，取**当月最后一个已发布工作日**的值。
- 看板「截止日」选取时，若该日无数据，自动顺延到最近一个已发布工作日。

### 3. 月初月末取数（Excel 底稿）
- Excel 底稿按月份生成，月初值、月末值分别按上述节假日规则取数。
- 公式引用模板中的既有单元格（模板 `template/汇率底稿模版.xlsx` 内含公式结构）。

### 4. 直接 / 间接标价法
- **直接标价法（100 外币折合人民币）**：美元、欧元、日元、港元、英镑、澳元、新西兰元、新加坡元、瑞士法郎、加元（10 种）
- **间接标价法（100 人民币折合外币）**：澳门元、林吉特、卢布、兰特、韩元、迪拉姆、里亚尔、福林、兹罗提、丹麦克朗、瑞典克朗、挪威克朗、里拉、比索、泰铢（15 种）
- 看板按这两类分两个区块展示，卡片备注标明报价方式，与 SAFE 官方口径一致。

### 5. 滞后判定与标红
- 每个币种独立取「**截止日或之前最近一个已发布工作日**」的值，记录其真实 `asOf` 日期。
- 若 `asOf < 截止日`（即当天该汇率尚未发布），判定为**滞后**：
  - 卡片标红、显示 `(滞后)`
  - 区块标题汇总：`部分币种数据滞后: 美元(2026-07-23)`
- 这样能明确区分"当天数据"与"顺延数据"，避免误读。

### 6. CSV 导出 vs Excel 文件
- **Excel 底稿（13 个 .xlsx）**：服务器真实生成的文件，由工作流（每天/手动触发）重算；看板按钮**不动**它。
- **历史明细 CSV**：浏览器用当前页面已加载的 `data.json` 现场拼成，跟着页面数据走，**不依赖** .xlsx 文件。
- 因此：每天自动更新后，看板数字、CSV 导出、13 个 Excel **三者同步为最新**；仅当手动点看板按钮（未触发工作流）时，只有页面与 CSV 刷新，Excel 不动。

### 7. 数据新鲜度矩阵
| 操作 | 看板数字 | 历史 CSV | 13 个 Excel |
|------|----------|----------|-------------|
| 每天 18:00 自动跑 | ✅ 新 | ✅ 新 | ✅ 新 |
| GitHub Actions 手动 Run workflow | ✅ 新 | ✅ 新 | ✅ 新 |
| 看板「手动实时更新」按钮 | ✅ 刷新（已部署数据） | ✅ 同步 | ❌ 不更新 |

## 七、故障排查
- **网站打不开**：Settings → Pages → Source 是否选 `GitHub Actions`；Actions 页 deploy 是否绿勾；`Ctrl+F5` 强刷。
- **数据未更新**：检查 Actions 最近一次运行是否成功；定时任务受 UTC/北京时区换算影响（北京 18:00 = UTC 10:00）；非工作日不自动跑。
- **抓取失败**：对端网站改版（Ortax 为 Next.js 内嵌 JSON、SAFE 接口字段变化），需改 `scrapers.py` 后重新 Push。
- **Excel 公式异常**：确认 `template/汇率底稿模版.xlsx` 未被破坏；`regen_all.py` 用缓存生成不重抓，改模板需重跑 `main.py`。

## 八、可选：云端一键更新（当前未启用）
曾尝试用 Cloudflare Worker / 腾讯云 SCF 让网页按钮直接触发工作流（含 `cloudflare-worker.js`、`scf_trigger.py`）。因中国大陆网络访问 `*.workers.dev` 超时（`net::ERR_CONNECTION_TIMED_OUT`）而放弃；腾讯云方案需注册账号并配置 API 网关，用户选择暂不实施，维持「手动上 GitHub Actions 触发」方式。
如未来启用，把后端 URL 填入 `config.json.update_worker_url` 并重新部署即可，前端已预留 `cloudTrigger` 逻辑（当前因 URL 为空走本地刷新分支）。
