# 自动更新说明

汇率底稿系统已脱离 WorkBuddy 独立运行。只要电脑**开着并已登录 Windows**，无需打开任何软件，数据会自动更新并部署到：
https://Crazy-onion.github.io/exchange_rate_system/

---

## 一、怎么自动的？

| 组件 | 作用 |
|---|---|
| `watcher.py` | 后台哨兵。登录后自动启动，每 15 分钟巡检一次：只要线上数据还不是今天，就触发一次完整更新 |
| `daily_update.py` | 真正干活的：判断是否需要更新 → 刷新印尼兜底 → 提交 → 推送 → 轮询验证部署 |
| `start_watcher.vbs` | 静默启动器，放入 Windows 启动文件夹，登录即自动拉起哨兵 |
| GitHub Actions | 收到推送后重新抓取三大源、生成 Excel 与看板并发布到 Pages |

**为什么需要本机参与？**
印尼 Ortax 数据源对 GitHub 服务器（海外 IP）限制访问，只有国内网络能抓到。因此印尼汇率靠本機抓取后写入 `fallback/ortax_fallback.json` 再提交，服务器抓失败时自动降级使用。另外两个数据源（国家外汇管理局）服务器能正常抓取。

---

## 二、触发机制

- **每日时间点**：本机时间过了 **10:20** 之后，哨兵发现数据不是今天就会开跑（不再局限于某一刻，错过也能补）
- **失败退避**：某次失败后等 45 分钟重试，连续失败逐步延长间隔
- **单实例**：通过本地端口 47391 互斥，不会出现多个副本互相打架
- **额外保险**：GitHub Actions 自身也配了 10:30 / 12:30 / 14:30 三次定时（但该定时本身会延迟或被丢弃，只能当补充）

---

## 三、怎么确认它在工作？

1. 看日志：`exchange_rate_system/logs/update.log`（每次巡检都会留痕）
   - 正常：「巡检 14:48：数据已是最新，下次巡检 15 分钟后」
   - 干活：「巡检 10:30：需要更新 —— 线上数据不是今天」接着是刷新兜底、推送、部署完成
2. 看任务管理器：应有一个 **pythonw.exe** 进程常驻（看不到窗口，正常）
3. 看网页抬头「自动更新时间」是否为今天

---

## 四、出问题怎么办？

| 现象 | 处理 |
|---|---|
| 网页数据停在昨天 | 先等 15 分钟（哨兵下一轮巡检）；再看 `logs/update.log` 最后几行 |
| 日志长时间没新内容 | 哨兵没运行 —— 重启电脑或重新登录后会自动恢复 |
| 印尼那一栏为空 | 严重异常，检查 `fallback/ortax_fallback.json` 是否存在且非空 |
| 日志显示推送失败 | 多为网络波动，脚本会自动重试；持续失败时检查本机网络与 GitHub 登录状态 |

---

## 五、希望换回「Windows 计划任务」？

计划任务（开始时间精确、支持"错过后补跑"）需要**管理员权限**，当前账号无法直接注册。
若想启用，以管理员身份打开 PowerShell，执行 `windows_task.xml` 所在目录对应的命令：

```powershell
Register-ScheduledTask -TaskName "汇率底稿自动更新" `
  -Xml (Get-Content "C:\Users\rfuser\WorkBuddy\2026-07-24-15-41-07\exchange_rate_system\windows_task.xml" -Raw) -Force
```

注册成功后，记得把启动文件夹里的 `汇率底稿自动更新.vbs` 删掉，避免双份运行。
